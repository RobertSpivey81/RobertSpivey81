name: Update badges

on:
  schedule:
    # Runs at 0am UTC every day
    - cron: "35 12 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: RobertSpivey81/badge-readme@main
